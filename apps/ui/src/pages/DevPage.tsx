import { TransactionTrackerCard } from "components/transaction/TransactionTrackerCard"

export default function DevPage() {
  return (
    <main className="my-16 flex flex-col gap-y-24">
      <TransactionTrackerCard />
    </main>
  )
}
